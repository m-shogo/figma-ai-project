# Figma Update Snapshot — 2026-09-04

Evidence maturity: **E0 official release signal**

この repo の Mission は **Figma → AI 実装の First-pass Fidelity / Rework / 再現性** を上げること。Figma 製品の全部を追わない。

RETEST_CANDIDATE。Company Policy / Proven Playbook の自動変更ではない。
実 node で「実装の手戻りが減るか」が見えてから disposition する。

Official sources:

- https://www.figma.com/release-notes/
- https://developers.figma.com/docs/figma-mcp-server/
- https://developers.figma.com/docs/figma-mcp-server/tools-and-prompts/

Previous dated snapshot: `research/figma-updates/2026-08-10.md`（上書きしない）

Radar latest at write time: `2026-09-03T08:05:45+00:00`

## Mission に効く（Figma structured context → Web 実装）

### 1. Auto spacing Around / Evenly / Between（2026-08-21）

Figma layout を CSS `space-between` / `space-around` / `space-evenly` へ落とすときの誤訳が減る。first-pass の spacing rework 向け。

### 2. MCP 読み取り面（docs 2026-09 時点）

`get_design_context` / `download_assets`（export + raw source） / `get_metadata`（大きい frame の分割） / `get_motion_context`（prototype の hover/click を推測しない）。

短命 URL 直貼り禁止と納品 WebP は維持。公式 export は PNG/JPG/SVG/PDF のまま。

### 3. Text wrap Balance / Pretty（2026-08-14）

Figma にその指定があるときだけ CSS `text-wrap` へ。無いのに発明しない。ブラウザ契約未確認のまま default にしない。

### 4. Color variable opacity aliases（2026-09-03）

Variables を使っている reference の overlay / disabled を token のまま実装できる。無い file では発明しない。

### 5. Code Connect UI + MCP instructions

design system 案件の component reuse / first-pass。`CONDITIONAL`。clone しない。Classic Theme 案件へ強制しない。

### 6. Shader を MCP で exact source として読める（2026-09-01）

reference が shader を使っているとき、raster 化や CSS 再構成を先にしない。使っていなければ非該当。

## Mission 外（今は追わない）

- Weave / generative plugin で frozen reference を作り直す
- Figma agent chat の別ウィンドウ
- Draw eraser / paint bucket
- Enterprise MCP SSO
- Community への shader 公開
- whole-page one-shot を production default に戻す

## Disposition 待ち

CANDIDATE 化は、実装 run で該当 signal が node にあり、repair が減る証拠が残ってから。
