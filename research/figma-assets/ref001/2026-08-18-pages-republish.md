# REF-001 Pages republish

A small, non-runtime change used to force the GitHub Pages Human Review publish workflow after PR #154 was merged into `so` without a confirmed fresh public deployment.

Runtime authority remains the current `so` implementation. This file does not change REF-001 visual or interaction behavior.
