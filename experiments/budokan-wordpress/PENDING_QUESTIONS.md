# まとめて聞くメモ（ブロックしない）

Human 2026-09-15: 1問ずつ聞かない。ここに溜めて、区切りでまとめて出す。

- イベント Figma カードに日付レンジ（`～`）がある。契約フィールドは開催日1本。レンジはまだ出さない。
- 一覧の「開催日：」ラベルを Figma どおり出すか、値だけ出すか。 → archive は `2026年8月1日(土)`。TOP は既存「開催日 :」を維持し値だけ `event_date`。
- 「カレンダーで見る」は TOP `#top_calendar` へ。別カレンダーページは作らない。
- 募集中 / 開催中 / 受付終了チップはフィールド無し → 出さない。
- 詳細の会場・対象・申込締切・お申込みは本文（既存テーブル / CTA ボタン）。PHP で表を組まない。
- TOP イベント行は今 `get_the_date()`。詳細実装時に `event_date` へ切り替え予定。 → 切替済み。空なら行を出さない。
- 書写書道 single を公開メニューに出すか（現行バックは PDF only）。
- native `/budo-book/` `/event/` 以外の CPT archive の 301 / noindex。
- 総索引の出し方。
- 英語単行本 `/publications/budo/books-en/`。
